---
import Button from './Button.astro';
---

<section id="home" class="hero">
  <div class="hero-overlay"></div>
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <span class="subtitle">Sabor Autêntico</span>
        <h1>Bem-vindo à Casa da Gi</h1>
        <p>Experimente o melhor da culinária caseira com ingredientes frescos e receitas tradicionais que aquecem o coração.</p>
        <Button href="#contato" variant="primary" class="button-large">Reserve sua Mesa</Button>
      </div>
      <div class="hero-visual">
        <div class="floating-element float-1">
          <img src="./images/Servindo.png?height=200&width=200" alt="Prato delicioso" loading="eager" />
        </div>
        <div class="floating-element float-2">
          <img src="./images/Suco.jpg?height=150&width=150" alt="Ingredientes frescos" loading="eager" />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 90vh;
    display: flex;
    align-items: center;
    background: linear-gradient(135deg, var(--vinho-tinto) 0%, var(--vermelho-carmesim) 100%);
    overflow: hidden;
    margin-top: 90px;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url('/placeholder.svg?height=1080&width=1920');
    background-size: cover;
    background-position: center;
    opacity: 0.15;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(166, 50, 71, 0.8) 0%, rgba(217, 41, 56, 0.7) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 55% 45%;
    gap: 60px;
    align-items: center;
  }

  .hero-text {
    color: var(--branco);
  }

  .subtitle {
    display: inline-block;
    color: var(--ouro-mostarda);
    font-size: 18px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 20px;
  }

  .hero-text h1 {
    font-size: 68px;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 24px;
    color: var(--branco);
  }

  .hero-text p {
    font-size: 18px;
    line-height: 1.7;
    margin-bottom: 40px;
    opacity: 0.95;
  }

  .hero-visual {
    position: relative;
    height: 500px;
  }

  .floating-element {
    position: absolute;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .floating-element img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .float-1 {
    top: 50px;
    right: 100px;
    width: 280px;
    height: 280px;
    animation: float 4s ease-in-out infinite;
  }

  .float-2 {
    bottom: 80px;
    right: 50px;
    width: 200px;
    height: 200px;
    animation: float 5s ease-in-out infinite 1s;
  }

  @media (max-width: 1024px) {
    .hero-text h1 {
      font-size: 52px;
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .hero-visual {
      height: 350px;
    }
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 70vh;
      margin-top: 70px;
    }

    .hero-text h1 {
      font-size: 40px;
    }

    .hero-text p {
      font-size: 16px;
    }

    .subtitle {
      font-size: 14px;
    }

    .hero-visual {
      display: none;
    }
  }
</style>
